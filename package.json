{
  "name": "agriconnect-ai-week2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "backend": "node backend/server.js",
    "start": "concurrently -n backend,frontend -c green,cyan \"npm:backend\" \"npm:dev\"",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^8.1.3"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
